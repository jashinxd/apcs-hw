public class Driver {
    public static void main(String[] args) {
	String s = "jason shin";
	int space = indexOf(" ");
	String first = s.substring( 1, space);
	String last = s.substring( space, s.length());
	
	System.out.println(first);
	System.out.println(last);
    }
}
